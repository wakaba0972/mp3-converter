# MP4 轉 MP3 Android App — MVP 開發規格

## 1. 專案目標

建立一款可在 Android 裝置上執行的 MP4 轉 MP3 App。使用者能從手機選擇 MP4 檔案，在裝置本機完成音訊轉換，並播放或分享產生的 MP3。

本專案使用 Flutter 與 Dart 開發，日常功能開發不編寫 Java 或 Kotlin。影片不會上傳至伺服器，以保障隱私並避免網路依賴。

## 2. MVP 範圍

MVP 僅處理單一 MP4 檔案，完整支援以下主要流程：

1. 選擇 MP4 檔案。
2. 確認檔案資訊並設定輸出檔名及音質。
3. 開始轉換並查看進度。
4. 視需要取消轉換。
5. 轉換完成後播放、分享或開始下一次轉換。

## 3. 功能需求

### 3.1 選擇來源檔案

- 使用 Android 系統檔案選擇器選擇單一 `.mp4` 檔案。
- 顯示來源檔案名稱與檔案大小。
- 使用者取消選擇時，App 應維持可操作狀態，不顯示錯誤。
- 非 MP4、無法讀取或已不存在的檔案不得進入轉換流程。
- 選擇新檔案後，清除前一次的轉換狀態及輸出結果。

### 3.2 輸出設定

- 預設輸出檔名沿用來源檔名，但移除 `.mp4` 副檔名。
- 允許使用者修改輸出檔名。
- 自動移除使用者輸入中重複的 `.mp3` 副檔名。
- 禁止空白檔名及 Android 不支援的檔名字元。
- 提供下列 MP3 位元率：
  - 128 kbps：較小檔案。
  - 192 kbps：預設選項，平衡音質與容量。
  - 320 kbps：較高音質。

### 3.3 執行轉換

- 轉換必須完全在裝置本機完成。
- 只擷取音訊，不保留影片畫面。
- 轉換期間禁止重複啟動另一個轉換工作。
- App 必須呈現以下狀態：
  - 尚未選擇檔案。
  - 已選擇、等待轉換。
  - 轉換中。
  - 轉換完成。
  - 轉換失敗。
  - 已取消。
- 若來源影片沒有音軌，應停止轉換並顯示可理解的訊息。

### 3.4 進度與取消

- 轉換期間顯示進度條與百分比。
- 若可取得影片長度，顯示已處理時間與總長度。
- 提供取消按鈕。
- 取消後停止轉換程序，並刪除未完成的輸出檔案。
- 取消操作不得造成 App 閃退或留下無法使用的 MP3。

### 3.5 儲存輸出

- 將 MP3 儲存在 Android 使用者可存取的位置。
- 完成後顯示輸出檔名與儲存位置。
- 不覆蓋既有檔案；若檔名重複，自動加上流水號，例如 `audio (1).mp3`。
- 儲存失敗或空間不足時，顯示原因並保留重新操作的能力。

### 3.6 完成後操作

- 使用系統支援的 App 播放或開啟 MP3。
- 透過 Android 分享介面分享 MP3。
- 提供「再轉換一個」操作，將畫面重設為初始狀態。

## 4. 使用者介面

MVP 採用單頁流程，畫面依狀態更新，不需要導覽至多個頁面。

```text
MP4 轉 MP3

[ 選擇 MP4 檔案 ]

影片：example.mp4
大小：35.2 MB

輸出檔名
[ example____________ ] .mp3

音質
( ) 128 kbps  (•) 192 kbps  ( ) 320 kbps

[ 開始轉換 ]

████████░░ 80%
正在轉換… 00:48 / 01:00
[ 取消 ]

轉換完成
[ 播放 ] [ 分享 ] [ 再轉換一個 ]
```

### 4.1 基本互動規則

- 未選擇有效 MP4 前，「開始轉換」按鈕不可用。
- 輸出檔名無效時，在欄位附近顯示錯誤並禁止轉換。
- 轉換中鎖定檔案、檔名與音質設定。
- 成功、失敗和取消狀態須以文字呈現，不能只依賴顏色。
- 所有主要按鈕需具備足夠觸控範圍及無障礙標籤。

## 5. 錯誤處理

App 至少必須安全處理以下情況：

- 使用者取消檔案選擇。
- 選擇非 MP4 或無法讀取的檔案。
- 來源檔案在轉換前被移動或刪除。
- MP4 不含音軌或內容損壞。
- Android 權限或輸出位置不可用。
- 儲存空間不足。
- FFmpeg 初始化或轉換失敗。
- 使用者取消正在進行的轉換。
- App 在轉換期間進入背景或畫面被重建。

錯誤訊息應說明發生什麼問題及使用者可採取的下一步，且任何錯誤都不得造成 App 閃退。

## 6. 技術方向

- UI 與應用邏輯：Flutter、Dart。
- 檔案選擇：Flutter 檔案選擇套件。
- 本機轉換：支援目前 Android 規範的 FFmpeg Flutter 套件。
- 檔案位置：Android Storage Access Framework 或 MediaStore 相容方案。
- 分享與開啟檔案：Flutter 平台套件。
- 狀態管理：MVP 優先採用簡單、可測試且不過度設計的方案。

選定 FFmpeg 套件前，必須確認：

- 套件仍持續維護。
- 支援目標 Android SDK 與 16 KB memory page。
- 包含 MP3 編碼能力。
- 授權符合專案發布方式；除非專案明確接受 GPL，否則不得引入 GPL 版本。

## 7. 測試需求

每次功能改動都必須新增或更新相關測試。MVP 至少需要：

### 7.1 單元測試

- 從來源檔名產生預設輸出檔名。
- 清理及驗證輸出檔名。
- 位元率選項對應正確的轉換參數。
- 同名檔案流水號產生規則。
- 轉換狀態切換。
- 進度數值計算及範圍限制。
- FFmpeg 成功、失敗與取消結果的處理。

### 7.2 Widget 測試

- 初始畫面正確顯示。
- 未選擇檔案時無法開始轉換。
- 選擇檔案後顯示正確資訊與預設值。
- 無效檔名顯示驗證訊息。
- 轉換中鎖定設定並顯示取消按鈕。
- 成功後顯示播放、分享與重新開始操作。
- 失敗後顯示錯誤且允許重試。

### 7.3 整合與裝置驗證

- 在 Android 模擬器或實體裝置選擇 MP4。
- 以 128、192、320 kbps 各完成一次轉換。
- 確認產生的 MP3 可正常播放。
- 驗證取消轉換不留下未完成檔案。
- 驗證同名輸出不覆蓋既有檔案。
- 驗證分享功能能開啟 Android 分享介面。
- 使用至少一個沒有音軌或已損壞的測試檔案驗證錯誤處理。

## 8. MVP 驗收標準

符合以下所有條件才可視為完成：

- 可選擇一般 MP4 並產生可播放的 MP3。
- 128、192、320 kbps 三種設定皆可正常使用。
- 進度與轉換狀態正確更新。
- 可取消轉換，且不留下損壞的輸出檔案。
- 不會覆蓋既有的同名檔案。
- 完成後可播放或分享 MP3。
- 已涵蓋主要錯誤情境，且 App 不會因此閃退。
- 所有單元測試、Widget 測試、整合驗證與靜態分析均通過。
- 可產生並在 Android 裝置安裝執行的 APK。

## 9. MVP 不包含的功能

以下項目延後至後續版本：

- 多檔案批次轉換。
- 選取影片片段或剪輯音訊。
- 自訂取樣率、聲道及進階編碼參數。
- 轉換歷史紀錄與檔案管理器。
- 長時間背景服務及通知列進度。
- 從 YouTube 或其他網址下載影片。
- 帳號、登入及雲端同步。
- 廣告、訂閱及付費功能。
- iOS 版本。

## 10. 開發完成定義

每個功能交付前必須同時符合：

1. 功能實作完成並符合本文件的行為定義。
2. 已新增或更新相關測試。
3. 程式碼格式化、靜態分析及所有測試通過。
4. 已完成必要的 Android 模擬器或實體裝置驗證。
5. 沒有未說明的已知重大錯誤。
6. 已使用清楚的提交訊息完成 `git commit`。
